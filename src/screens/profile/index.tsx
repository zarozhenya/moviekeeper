import React, {FC} from 'react';
import {SafeAreaView, Text} from 'react-native';

export const ProfileScreen: FC = () => {
  return (
    <SafeAreaView>
      <Text>Profile</Text>
    </SafeAreaView>
  );
};
