import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  inputContainer: {
    paddingVertical: 12,
    paddingLeft: 12,
    paddingRight: 44,
    borderColor: '#000000',
    borderWidth: 0.5,
    borderRadius: 8,
  },
  input: {
    fontSize: 14,
    lineHeight: 20,
    color: '#000000',
  },
  inputAccent: {
    borderColor: '#ff6b08',
  },
  endContentContainer: {
    position: 'absolute',
    top: 12,
    right: 12,
  },
});
