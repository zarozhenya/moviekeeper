import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  input: {
    fontSize: 14,
    color: '#000000',
    borderWidth: 0.5,
    borderColor: '#000000',
    borderRadius: 8,
    height: 40,
    paddingHorizontal: 12,
    marginBottom: 16,
  },
  inputAccent: {
    borderColor: '#ff6b08',
  },
});
